# TableCheck VIP Floor Gate C source-lineage v6r5 — Vercel auth recovery and reconciliation continuation prompt

Date: 2026-07-21 JST

Status: new-session execution handoff for local Vercel credential recovery, authenticated read-only readiness,
additive v6r5 reconciliation tooling, fresh exact three-role review, and conditional continuation execution. The
existing v4 Owner authority remains the only provider-mutation authority. This prompt does not widen its scope.

## How to use

Start a new primary-agent session in the GHOST repository root containing both `AGENTS.md` and `website/`.

Give the agent everything between **Prompt starts** and **Prompt ends**. The agent may complete all local and
read-only work autonomously. If the Vercel credential must be replaced, the Owner performs that credential entry
directly in a separate local terminal or Vercel device flow; the token value, device URL/code, raw provider URL,
and provider IDs must never be sent through chat or an agent tool call.

The exact v4 authority already permits the reconciliation scope below when every frozen byte and identity remains
exact. The v6r4 attempt consumed no v4 provider-mutation ceiling and recorded no uncertain outcome. Do not ask the
Owner to repeat the v4 role code merely because the local Vercel credential expired or lost Team scope. Any scope
expansion, frozen-lineage drift, or unknown Stripe mutation outcome still requires a new exact proposal and
separate direct Owner authorization.

---

## Prompt starts

Resume TableCheck VIP Floor Gate C source-lineage reconciliation from the GHOST repository root containing
`AGENTS.md` and `website/` as the sole state-transition operator. Work persistently through the safe
credential checkpoint and all authorized steps until either:

1. Vercel authenticated read access is restored, additive v6r5 tooling is fully validated and frozen, exactly
   three independent read-only reviews ACCEPT the same immutable manifest, the exact v4 continuation completes,
   and one truthful additive atomic terminal supersedes the v6r4 auth-read HOLD; or
2. a truthful fail-closed HOLD/readiness handoff is published without exceeding any v4 ceiling.

Never report an unmet condition as PASS. Never deploy, redeploy, delete, promote, move an alias, change a Vercel
project/environment, or begin Phase D. Preserve all unrelated dirty work.

### 1. Read completely before acting

Read these files in full before planning, credential diagnostics, editing, review, or provider access:

- `AGENTS.md`
- `docs/AI_CURRENT_STATUS.md`
- the latest rows of `docs/AI_WORK_LOG.md`
- `docs/AI_AGENT_SYNC.md`
- `website/AGENTS.md`
- `.agents/skills/stripe-best-practices/SKILL.md`
- the Stripe security reference selected by that skill
- `website/docs/research/tablecheck-vip-floor-gate-c-source-lineage-v4-reconciliation-continuation-prompt-2026-07-21.md`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-continuation-authorization-proposal-v4-20260721.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-continuation-owner-authorization-v4-20260721T092458+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-terminal-hold-v5r10-20260721T001825.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-tooling-manifest-v5r10-20260721T090538+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-tooling-review-v5r10-accept-20260721T090857+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-runtime-source-artifact-freeze-v4-20260721T081722+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-provider-auth-read-hold-v6-20260721T100340+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-tooling-review-v6r3-reject-20260721T102035+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-tooling-verification-v6r4-20260721T102519+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-tooling-manifest-v6r4-20260721T102601+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-tooling-review-v6r4-accept-20260721T103148+0900.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-terminal-hold-v6-20260721T013310.json`
- `website/docs/evidence/vip-floor-v2/gate-c-source-lineage-reconciliation-execution-hold-v6r4-20260721T103431+0900.json`
- all v6 driver, freezer, verifier, audit, provider, executor, journal, Stripe profile, atomic publisher, runtime auth,
  privacy scanner, and recursively imported active dependencies
- this v6r5 prompt

Inspect the outer workspace and `website/` status. Use `rg` and `rg --files`. Do not commit, push, reset, clean,
delete or rewrite historical evidence, update dependencies, or touch unrelated files.

Use official Vercel documentation current at execution time to confirm the REST authentication, Team-scoped
Access Token, CLI device login, user/team/project/deployment/alias/file/custom-environment/environment-variable
read contracts. Use only official primary sources for technical behavior. Record citations in the final report,
but do not copy provider URLs into public project evidence.

### 2. Recompute the exact authority and starting bytes

Recompute rather than trust these SHA-256 values:

```text
v4 reconciliation proposal:
8db307c05fd331f4832160dd63abf9f4355098dee3f5e900fd76924ce8420fc6

v4 direct Owner authorization:
ff31f1cac6691a430aa50f160b778cd5dedbbfc880f79dcce4f7728fed705cfd

v5r10 historical terminal HOLD:
c985c0cedc9c1737cb36aa056e707f5a5f0ed863ee5303cdde0d7eee2efe5fc1

v5r10 deployed-tooling manifest:
adced7474461f21bf7b1a45ac779d01cbd9090aad04e78e0cdb1473303a304e3

v5r10 exact 3/3 ACCEPT review:
ca5a9780da348d99929d8087d93b0582778d0b91ec07add94c1b8dad6ece01aa

v6 provider-auth read HOLD:
e6cce92526d661239bafc512119db77f9d95549aaa01aacab18c7673e545b2de

v6r3 rejected review evidence:
c1523ce3b3b49bc05feeef3a6f3e5dfed7524bcfc6719819a56008f13ddb650d

v6r4 verification evidence:
d61fa397d362246958a8f4f0c4d3b44ff510ed73104cbfbf96b2690a23193aba

v6r4 reconciliation tooling manifest:
c25f36314bc58cbeec7a923c751ea91b3fcaf57fd8eac3bd8e3216ba24dc4b57

v6r4 exact 3/3 ACCEPT review:
0ebf8ea20363cb7620ffb539f66f985ec43347ffecddced41266eac1745ad2d6

v6r4 atomic terminal HOLD:
4b6be22a985f669a8d8620b916e57e03b7508b22526b785c77bdf09da9b0e319

v6r4 sanitized execution outcome:
8ad7ff6c6a145d4ebb337355fe18620dc9311c39c208a201bd6c80eb4a759d54
```

The exact existing deployment remains bound by:

```text
deployment identity SHA-256:
0ff543f3d83bda9dc3471162d2fa69871643cb82a66abe6f4d117f0de7ecee76

deployment host SHA-256:
1906925967f71b0ba763191de38a9feb003613322865218332053fde1c70536c

custom environment identity SHA-256:
a901c9cae16b3be6a1e8f9cfb4fcb31c7332b00d20831d5a0a06aeea0c49c38e

deploy action id:
5020866a9d93fe6885d0b0d6841967152129e28985726e47e1272d9dab389ddf

runtime source SHA-256:
6f821dd51261d1ab168467564ff216073a0640ed7eb3429d2e41a8c883a402bc

migration SHA-256:
c2e751f183b8fb35e5bcf468366622b9f19298d6c5c8e086702f9b1a5f193272

prebuilt artifact SHA-256:
3aea5d804f8cb60f6a84c554140f45f043ac7c5c1d7be7303e0da9a3e1c7e218

provider-comparable manifest SHA-256:
3141ccbc5759325294af9a02055f7e7957709bd23d5959e8c87e0539b00bfd7a

exact tenant issuer identity SHA-256:
795f00cdf7452ec9e132b836236aaf3927b1a227b2799a73adf1854274d34330

installed jose executable tree SHA-256:
c9c166bc8e3719bab85542a7af691993548a9e4309aff146d01af07e33c55e03
```

The direct Owner authorization role code remains exactly
`OWNER_GATE_C_SOURCE_LINEAGE_RECONCILIATION_CONTINUATION_AUTHORIZATION_V4`. Verify its same-principal reference
without reading the real name into output. The v6r4 outcome proves continuation journal rows 0, mutation attempts
0, uncertain outcomes 0, and every v4 provider-mutation count 0. Treat v6r4 as a completed fail-closed attempt,
not as permission to reuse its terminal-bound manifest.

If any authority, frozen runtime, migration, artifact, provider tree, issuer, historical evidence, or exact
deployment identity byte differs, stop without provider mutation. Do not rebuild or refreeze the deployed
artifact under v4.

### 3. Enforce the unchanged v4 ceilings

The entire v6r5 attempt, including all prior history, must enforce:

```text
historical total staging deploy count: 1
new staging deploy maximum under v4: 0
deployment delete maximum: 0
Production alias/promotion maximum: 0
existing test webhook forward update maximum: 1
webhook create maximum: 0
webhook delete maximum: 0
webhook signing-secret rotation maximum: 0
authenticated webhook compensation maximum: 1
DB write maximum: 0
Production mutation maximum: 0
Preview mutation maximum: 0
LINE mutation maximum: 0
UI mutation maximum: 0
customer-data mutation maximum: 0
business-cycle maximum: 0
live Stripe mutation maximum: 0
Phase D start: false
```

The v6r4 auth failure consumed none of these v4 maxima. Do not reinterpret that fact as permission to deploy,
delete, promote, modify aliases, edit Vercel configuration, or perform an arbitrary provider write.

### 4. Recover the Vercel credential through a safe human checkpoint

Vercel Access Tokens are created under a Personal Account and scoped to an account or Team. Do not invent
per-endpoint granular token permissions if the current Vercel product does not offer them. Use the narrowest
available scope that includes the one exact Team and project. The continuation code, not optimistic token scope,
must structurally restrict the operation surface to the approved reads and the separately authorized Stripe
webhook update.

Before touching the credential, validate the existing Vercel auth file without printing its path or contents:

- regular file owned by the current user;
- mode 0600;
- parent not group/world writable;
- no symlink;
- exactly one hard link;
- valid JSON containing a non-empty token;
- no ambient `VERCEL_TOKEN`, token CLI argument, shell history insertion, debug logging, or process-list exposure.

Never print, hash for public use, log, or publish the token. Never put it into command arguments, environment
variables visible to child processes, source, evidence, clipboard automation, or chat.

Run only a sanitized status-code-only authentication probe with the current credential. If it still returns
401/403, pause all subsequent steps and instruct the Owner exactly once:

```text
別のローカル端末で `vercel login` を実行し、表示されたdevice flowのlocation・IP・request timeを確認して承認してください。対象のexact Teamへアクセスできる同一Vercel principalを選び、完了後はtoken・URL・code・IDを貼らず「Vercelログイン完了」とだけ返信してください。
```

The Owner may instead create a new Account Token in the Vercel Dashboard scoped to the exact Team and install it
through their own secure local process. Do not ask how they stored it and do not ask them to paste it. Do not use
browser automation to capture a one-time token. Do not manually rewrite the auth file unless the Owner performs
the secret entry outside agent-visible tools.

After the Owner reports completion, revalidate the file boundary and identity through provider reads. Reject a
different personal account, Team, project, or missing project role. If secure recovery cannot be completed,
publish only sanitized auth-readiness evidence, update shared status/log, and stop with all external mutations 0.

### 5. Prove exhaustive Vercel read-only readiness before v6r5 work

Create or reuse a sanitized read-only verifier that reads credentials and linked identities internally. Provider
IDs and URLs must not appear in command arguments or public output. Store raw responses only in a newly created
private 0700 directory with 0600 files, owner/link/symlink checks, and no public path disclosure.

Allow only authenticated read operations needed to prove:

1. exact current User principal is authenticated without publishing username, email, or real name;
2. exhaustive Team inventory contains exactly one matching Team identity and yields the exact issuer identity;
3. exactly one linked Project identity/name matches the frozen one-way identities;
4. exhaustive Production, Preview, and custom `staging` deployment inventories are readable with Team scope;
5. the exact existing deployment detail is readable and still READY;
6. exactly one deployment alias is readable and its resolution is readable;
7. the exact deployment file tree is readable and remains 1953 files;
8. the exact custom `staging` environment record and current deployment aliases are readable;
9. Production, Preview, and custom-staging environment-variable inventories/configuration needed by the frozen
   verifier are readable; any decrypted values remain private and are never published;
10. pagination is exhausted, cursor loops/repeated pages/duplicates/truncation are rejected, and every request is
    bound to the exact Team/project.

Use the documented Team selector on Team resources. A 200 response is insufficient: validate response schema,
exact one-way identity, expected ownership, pagination exhaustion, and no error envelope. Public readiness
evidence may contain only endpoint-category booleans, status codes, counts, one-way hashes, and mutation count 0.

During this readiness phase require all of these to remain 0:

- Vercel POST/PATCH/PUT/DELETE calls;
- deploy/redeploy/promote/rollback/delete calls;
- alias assign/move/remove calls;
- environment-variable create/edit/delete calls;
- Stripe API calls and webhook updates;
- Supabase calls and DB writes;
- runtime protected-URL calls;
- Production/Preview/LINE/UI/customer/business-cycle mutations.

Add negative fixtures for 401, 403, wrong Team scope, wrong project, disabled/inactive credential, missing token,
ambient token, malformed responses, pagination faults, and accidental non-GET provider methods. Fixtures must
count actual provider/network and mutation calls, not merely assert a final public count.

Freeze sanitized evidence named additively for `vercel-auth-readiness-v6r5`. If every required category is not
authenticated and exact, do not create a mutation-capable v6r5 manifest and do not run any Stripe read/write.

### 6. Build an additive v6r5 candidate without changing v6 history

After Vercel readiness passes, create additive v6r5 entry points and evidence. Do not edit or replace v6r3/v6r4
manifest-bound files. Prefer new `v6r5` driver, freezer, verifier, audit, provider wrapper, private journal/lock,
and terminal resolver names. Reuse immutable v6 code only by imports whose exact bytes are included in the new
manifest.

The v6r5 candidate must:

- include the v6r4 manifest, review, terminal HOLD, execution outcome, provider-auth HOLD, v6r3 rejection, v6r4
  verification, and this prompt as historical inputs;
- use a fresh private continuation journal and lock bound to the v6r5 manifest;
- keep the v6r4 zero-row journal and all v6r4 public evidence immutable;
- structurally expose no deploy or deployment-delete method, including stale/resume/error/compensation paths;
- keep provider alias APIs read-only and derive custom-staging/Production/Preview/branch/unknown classification
  from authenticated provider data rather than hardcoded booleans;
- bind the fresh Stripe webhook baseline exactly to the authorized legacy prior private target before any write;
- keep credential bytes and credential hashes out of the manifest; freeze only verifier code, boundary policy,
  sanitized auth-readiness evidence, and one-way resource identities;
- reject execution if the Vercel credential becomes unreadable or changes to the wrong identity after freeze;
- retain direct Stripe HTTPS with API version `2026-04-22.dahlia`, the dedicated future-valid test restricted key,
  exact account/endpoint identities, exact 13-event set, and webhook signature/secret non-rotation boundaries;
- never pass Stripe credentials or webhook URLs in command arguments or logs.

The v6r4 terminal is immutable and truthfully described itself as current when published. A new v6r5 terminal
must add `supersedesTerminalSha256` bound to
`4b6be22a985f669a8d8620b916e57e03b7508b22526b785c77bdf09da9b0e319`. Implement an additive terminal-chain
resolver where the unique valid unsuperseded leaf is current. Reject forks, cycles, missing ancestors, multiple
leaves, manifest mismatch, or direct edits to old terminal bytes. Atomically publish only the new leaf by
same-filesystem rename. Do not rewrite `current` inside historical JSON.

### 7. Re-run the complete validation suite

Run and preserve exact exit codes for at least:

- `node --check` over every v6r5 entry point and active JavaScript module;
- target ESLint with zero errors and warnings;
- `npx tsc --noEmit`;
- VIP implementation static verification;
- VIP Floor v2 API static verification;
- runtime OIDC fixtures, including exact Team/project/issuer and expiration negatives;
- every applicable v5 fixture in remediation/applicable mode;
- every v6 fixture;
- new v6r5 Vercel credential/readiness fixtures;
- exact deployment/action/environment/source/artifact/provider/issuer/tooling negatives;
- zero/multiple/user-assigned/undeclared/wrong-target/Production/Preview/branch/unknown alias negatives;
- raw Preview 3-to-4 exact-identity exception and every other Preview/pagination negative;
- legacy webhook baseline drift and canonical target negatives;
- Stripe zero/multiple/live/account/profile/event-set/ambient/expired credential negatives;
- webhook response-loss, reconciliation, compensation, and crash-resume negatives;
- deploy/delete booby traps that fail before any provider call;
- complete recursive import-closure omission negative;
- credential/private journal mode, owner, hard-link, symlink, lock, and hash-chain negatives;
- terminal supersession-chain fork/cycle/multiple-leaf/atomic-publication negatives;
- public privacy negative and real scans.

Do not repair a failing test by weakening its assertion or reporting a fixture name that was not executed. Any
tooling repair is additive and requires the full suite again. A frozen runtime/source/artifact/provider change
invalidates v4 and requires HOLD/new exact authorization.

### 8. Freeze a new complete v6r5 manifest

Only after all required Vercel reads and local validations pass, freeze one new immutable v6r5 reconciliation
manifest. The terminal-bound v6r4 manifest must not be reused.

The manifest must recursively include every execution-relevant byte and reject omissions, including:

- all v6r5 and imported immutable v6 execution code;
- Vercel auth-readiness verifier and sanitized PASS evidence;
- driver, provider, executor, journal/lock, Stripe profile, final publisher, terminal-chain resolver, privacy
  scanner, preflight, normal/require-pass audit, fixtures, runtime auth/flags, and configs;
- this prompt and all exact v4/v5r10/v6r3/v6r4 historical evidence named above;
- exact proposal/authorization and all one-way operator/deployment/environment/account/webhook/bypass/issuer
  identities;
- exact fixed binaries, complete required package/dependency trees, and installed `jose` executable closure;
- unchanged ceilings and the distinction between deployed-tooling and operator reconciliation-tooling manifests.

Recompute all file, binary, package, dependency-tree, and closure hashes after freeze. Run the manifest verifier
against the exact output path and SHA. Run the real privacy scanner; public findings must be 0. The manifest must
record Vercel readiness PASS but must contain no credential byte/hash, raw identity, URL, response, or private
path.

### 9. Obtain exactly three fresh independent read-only reviews

Give the exact same immutable v6r5 manifest bytes to exactly these roles:

1. `provenance`
2. `runtime`
3. `security`

This explicit requirement authorizes the primary agent to delegate exactly those three bounded review tasks.
Each reviewer must be independent and read-only, recompute all referenced hashes, edit no file, receive no
credential/raw private value, make no provider/network call, and return exactly:

```text
independent: true
readOnly: true
decision: ACCEPT
blockingFindingCount: 0
providerOrNetworkCalls: 0
fileEdits: 0
```

Any REJECT retires that candidate with external mutation 0. Preserve a versioned rejection, repair additively,
rerun the full validation/readiness suite, freeze a new manifest, and obtain fresh reviews from all three roles.
Never carry an ACCEPT to new bytes.

### 10. Run local preflight and execute only after exact 3/3 ACCEPT

After and only after exact 3/3 ACCEPT:

1. Reverify v4 proposal/authorization, every frozen lineage SHA, v6r4 terminal/outcome, Vercel readiness evidence,
   v6r5 manifest/review bytes, complete import closure, fixed binaries/trees, credential boundaries, and the
   dedicated Stripe test restricted profile.
2. Run the frozen v6r5 local preflight. It must return `READY_TO_RECONCILE`, bind the exact v6r5 manifest/review,
   report deploy/delete callable path count 0, and perform no provider mutation.
3. Immediately re-read the Vercel User/Team/Project identity and reject credential drift.
4. Use only the frozen v6r5 driver for execution.

The execution order is:

1. Authenticated exhaustive Production/Preview/LINE and exact Preview-environment baselines.
2. Staging DB/migrations/dual-write and all-nine project-config/runtime flag baselines.
3. Exactly one Stripe test webhook, exact account/profile and exact 13-event set; preserve its private prior target
   only in the 0600 journal.
4. Exact existing deployment/action reconciliation through authenticated exhaustive readback, with no deploy or
   delete call.
5. Exact provider-declared custom-staging alias classification: alias 1, Production alias/promotion 0, Preview/
   branch/unknown alias 0, exact deployment resolution.
6. Raw Preview count 4 and invariant Preview count 3 only by removing the exact authorized deployment identity;
   all remaining identities and normalized hashes must equal the frozen baseline.
7. Provider artifact tree equality and exact file count 1953.
8. Runtime HTTP 200 with exact deployment/source/target/issuer, mandatory OIDC claims/expiration, all nine flags
   configured false/raw-disabled, DB dual-write false, migrations exact, DB writes 0.
9. Privately construct and validate the canonical exact deployment-host webhook URL. Never substitute the alias.
10. Update the one existing Stripe test webhook forward URL at most once. Never create/delete a webhook, rotate
    the signing secret, touch live mode, or perform any other Stripe mutation.
11. Authenticated webhook readback: exact account, endpoint count 1, event count/set 13, exact target.
12. Re-read all Production/Preview/LINE/Preview-environment/DB/runtime/alias/deployment state and enforce every
    zero-mutation ceiling.

Journal intent before the single Stripe update and authenticated receipt/readback afterward. On response loss,
reconcile the exact existing endpoint before any retry. An unprovable outcome consumes the one forward maximum,
enters `uncertainOutcomes`, forbids retry under this authority, and requires HOLD/new authorization.

If a proven failure occurs after the webhook changed, use authenticated compensation at most once to restore the
exact prior private target. Journal intent before compensation and reconcile/read back afterward. An unprovable
compensation consumes its maximum, forbids retry, and publishes only an unknown-outcome HOLD.

### 11. Complete fresh final readback, privacy, audits, and atomic terminal

After successful forward reconciliation:

1. Perform fresh authenticated deployment, alias, provider-tree, runtime, DB, Production, Preview, LINE, and
   Stripe webhook readbacks.
2. Run the standard preflight bound to exact final evidence, v6r5 manifest/review, deployment/action/source/
   artifact/provider/issuer/tooling identities, and zero deploy/delete counts.
3. Run the executable public privacy scanner over every candidate; all findings must be 0.
4. Run normal audit and preserve its real exit code and `auditValid`.
5. Run require-pass audit and preserve its real exit code and `auditValid`.
6. Perform one last authenticated full readback after the audits.
7. Atomically publish exactly one new v6r5 terminal-chain leaf with the v6r4 terminal SHA as its ancestor.

For successful source-lineage reconciliation under the current Gate C contract, normal audit is expected to exit
0 with `auditValid=true`; require-pass is expected to exit 2 with `auditValid=true` because separately governed
business-cycle/signature requirements remain. Do not force expected results. Any mismatch publishes truthful
HOLD with actual exits and counts.

### 12. Privacy and secret boundaries

- Private operational directories are owner-only mode 0700.
- Credentials, raw provider responses, URLs/IDs, OIDC/bypass values, prior/current webhook targets, environment
  values, and journals are owner-only mode 0600, one hard link, never symlinks.
- Never print or publish token values, token hashes, authorization headers, device URLs/codes, usernames/emails,
  raw team/project/deployment/account/endpoint IDs, issuer/webhook/deployment URLs, query values, customer data,
  notes, ciphertext, secrets, or private paths.
- Never place credentials, provider IDs, or private URLs in command arguments.
- Reject ambient Vercel and Stripe credentials.
- Use Stripe API version `2026-04-22.dahlia`, a dedicated future-valid test RAK, and exact least-privilege account/
  webhook permissions. Do not use an `sk_` key and do not touch live mode.
- Continue verifying Stripe webhook signatures in runtime code; never rotate or expose the signing secret.
- Public evidence contains only allowlisted booleans, counts, decisions, timestamps, exit codes, schema names, and
  one-way resource/evidence hashes.

### 13. Gate C and Phase D remain separately governed

A successful source-lineage result is `LINEAGE_RECONCILED_GATE_C_HOLD`, not Gate C PASS. This v4 scope does not
authorize:

- governed runtime business-cycle execution or evidence;
- final Gate C signatures;
- any scope expansion needed for those tasks;
- Phase D start.

Keep Gate C `HOLD` and `phaseDStarted=false` until the separately governed business-cycle evidence and required
signatures are exactly complete. Obtain a separate exact Owner authorization if that later scope requires one.
Start Phase D only after a truthful Gate C PASS.

### 14. Durable evidence, shared handoff, and final report

Create additive sanitized evidence for Vercel auth readiness, v6r5 verification, every review round, preflight,
reconciliation, alias/Preview classification, webhook/compensation, final readbacks, privacy, audits, and terminal.
Preserve actual exit codes and conservative counts.

Update `docs/AI_CURRENT_STATUS.md` whenever the active state or next action changes. Append a compact row to
`docs/AI_WORK_LOG.md` at each durable checkpoint and before the final response.

Report concisely:

- exact v4 proposal/authorization paths and SHAs;
- Vercel credential recovery method category without token/path/identity disclosure;
- authenticated User/Team/Project/Deployments/Aliases/Files/Custom Environments/Environment Variables readiness;
- v6r5 manifest path/SHA and file/binary/tree counts;
- provenance/runtime/security decisions and review evidence SHA;
- local preflight exit/decision/SHA;
- historical deploy total and v4 new deploy/delete/webhook forward/compensation/create/delete/rotation counts;
- exact alias classification and raw/invariant Preview results;
- provider artifact count/tree, runtime HTTP/source/target/issuer/flags, DB/migration/dual-write results;
- Stripe test endpoint/event-set/target readback;
- Production/Preview/LINE/UI/DB/customer/business-cycle mutation counts;
- privacy findings, normal/require-pass audit exits and `auditValid`;
- uncertain outcomes and the atomic terminal path/SHA/superseded ancestor;
- current Gate C decision and whether Phase D started.

If blocked, state the exact sanitized blocker and every confirmed mutation count. Never ask the Owner to disclose
a token, device URL/code, raw provider URL/ID, credential path, Stripe key/signature, or private operational path.

## Prompt ends
